[package]
name = "skein-qos"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
loom = { version = "0.7", optional = true }

[features]
default = []
loom-tests = ["dep:loom"]
