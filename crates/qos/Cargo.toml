[package]
name = "skein-qos"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libc = "0.2"
loom = { version = "0.7", optional = true }

[features]
default = []
loom-tests = ["dep:loom"]
