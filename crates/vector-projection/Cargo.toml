[package]
name = "skein-vector-projection"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
crc32fast = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
skein-core = { path = "../core" }
