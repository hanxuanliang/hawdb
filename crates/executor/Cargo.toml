[package]
name = "skein-executor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
skein-core = { path = "../core" }
skein-plan = { path = "../plan" }
skein-storage = { path = "../storage" }
